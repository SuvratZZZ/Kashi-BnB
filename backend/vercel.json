{
  "version": 2,
  "build": "npm install && npx prisma generate",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
      {
        "src": "/(.*)",
        "dest": "index.js"
      }
    ]
  }
